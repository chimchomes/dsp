# commit

# Commit and Push Changes
- **Action**: 
    1. Run `git add .`
    2. Generate a professional commit message based on the changes.
    3. Run `git commit -m "[Generated Message]"`
    4. Run `git push`
- **Condition**: Only run this if the user says the changes have been tested and are working.